#include <stdio.h>

int main()
{
    int num1, num2, num;
    char AS;
    printf("Enther the first number :");
    scanf("%d", &num1);

    printf("Select the operator you need (+,-,/,%%,*) :\n");
    scanf("%c", &AS);

    printf("Enter the secound number :\n");
    scanf("%d", &num2);

        return 0;
}